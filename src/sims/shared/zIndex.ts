/** Documented stacking layers for SimLab shared UI. */
export const Z_INDEX = {
  /** Portaled info popovers and their backdrops — above sim chrome, panels, and charts. */
  popover: 1000,
} as const
